#include <tunables/global>

profile e975af66_hacc flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Allow all capabilities for full access
  capability,

  # Allow all file access
  /** rwmlkix,

  # Allow network access
  network,

  # Allow signals
  signal,

  # Allow ptrace for debugging
  ptrace,

  # Allow unix sockets
  unix,

  # s6-overlay v3 paths
  /run/s6/** rwix,
  /run/s6-rc* rwix,
  /package/** rx,
  /command/** rx,
}
